<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:loc="clr-namespace:FrozenSky.RKKinectLounge.Modules.Multimedia">
    
    <loc:DesignModeViewModels x:Key="DesignVM" />
    
    <!-- Generic animations and styles-->
    <Storyboard x:Key="FadeInAnimation" AutoReverse="False">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)">
            <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
            <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1"/>
        </DoubleAnimationUsingKeyFrames>
        <ThicknessAnimationUsingKeyFrames Storyboard.TargetProperty="(FrameworkElement.Margin)">
            <EasingThicknessKeyFrame KeyTime="0" Value="20,60,20,-20"/>
            <EasingThicknessKeyFrame KeyTime="0:0:0.3" Value="20"/>
        </ThicknessAnimationUsingKeyFrames>
    </Storyboard>

    <!-- Binding the animation to displayed Borders (see DataTemplate) -->
    <Style TargetType="Border" x:Key="AnimatedBorder">
        <Style.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
            </EventTrigger>
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>